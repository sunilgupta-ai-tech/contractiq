# --- Web ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-multipart==0.0.20

# --- Database ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# --- Cache / queue ---
redis==5.2.1
arq==0.26.1

# --- Vector store ---
qdrant-client==1.12.1

# --- Security ---
pyjwt==2.10.1
bcrypt==4.2.1

# --- Storage ---
boto3==1.35.90

# --- HTTP ---
httpx==0.28.1

# --- Document processing (Phase 4) ---
# PyMuPDF is AGPL-3.0: closed-source commercial use needs an Artifex licence.
pymupdf==1.28.2
pdfplumber==0.11.10
# Python wrapper only; the `tesseract` binary is installed in workers/Dockerfile.
pytesseract==0.3.13
pillow==12.3.0
# unstructured[pdf] deliberately not used — see app/document_processing/unstructured_parser.py

# --- Agent workflow (Phase 8) ---
# LangGraph only (it brings langchain-core). The agent's nodes call our own
# Gemini/Ollama providers (app/llm), so langchain, langchain-google-genai and
# langchain-ollama are not needed — one LLM layer, not two.
langgraph==1.2.12
